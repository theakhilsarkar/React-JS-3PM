// html - documentaion
// informative website
// backend services - 
// json server - pc,
// firebase - cloud(accessible everywhere) storage

// client --> api --> server(store & manage) : database(store)
// client --> firebase

// database:
// is collection of data which is stored in systamatically.

// 1. relational database 
// -> data stored in tabular format. (table: row & col)
// -> sql 
// -> if your data formated/fixed/structured

// 2. non-relational database
// -> data stored in key & values, document & collection
// -> firebase, json-server
// -> if your data is dynamic,unstructured

// social media - insta

// Relational Database Management System
// DBMS

// firebase
// authentication - google
// firestore-database - non-relationally - document-collection : text/number
// firebase storage - files : paid

// firestore
// collection -> document(key-value) -> collection -> document

// folder->files->information(key-value)