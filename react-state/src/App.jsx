import "./App.css";
import Counter from "./components/Counter";

function App() {
  return (
    <>
      {/* {users.map((user) => (
        <User name={user.name} bio={user.email} />
      ))} */}
      <Counter />
    </>
  );
}

export default App;
